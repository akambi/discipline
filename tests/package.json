{
  "name": "discipline-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --testEnvironment node"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "moduleDirectories": ["node_modules", "../node_modules"],
    "transformIgnorePatterns": [
      "/node_modules/(?!(@aws-sdk|@smithy)/)"
    ]
  }
}
